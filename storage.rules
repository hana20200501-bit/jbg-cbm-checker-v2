rules_version = '2';

// Allow public read and write access to the box-images directory
service firebase.storage {
  match /b/{bucket}/o {
    match /box-images/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
